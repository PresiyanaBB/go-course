package main

//"fmt"

func main() {
	// reader := bufio.NewReader(os.Stdin)
	// var uniqueValues map[string]bool
	// arr, _ := reader.ReadString('\n')
	// for _, symbol := range arr {
	// 	if !uniqueValues[symbol] {

	// 	}
	// }
}
